package com.helloblog.service;

import com.helloblog.domain.BloggerImage;

public interface BloggerImageService {

    int addOneImage(BloggerImage bloggerImage);

    Integer isExitImage(BloggerImage bloggerImage);

}
